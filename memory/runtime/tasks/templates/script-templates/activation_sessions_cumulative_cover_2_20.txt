# Append this block to EVERY session user message (same chat rules as activation_session_commands.txt).
# It asks the model to prove wiring + disk reads without dumping the whole system prompt.

---BEGIN VERIFICATION (append to each session)---

**Phased activation:** If this chat is part of the 20-session activation sequence, you must already have output **Handoff** ( **A. Outstanding tasks** + **B.** next-session or post-activation steps, both numbered ) **above** this section. **Verification** comes **after** **Handoff**, never instead of it.

Before you finish your reply, add a section **"Verification"** with:

1. **HERMES_HOME** — Absolute path and active profile name (e.g. chief-orchestrator).
2. **Path wiring** — Read `HERMES_HOME/.hermes.md` with file tools; print the resolved absolute paths for the policy root and workspace (as stated in that file — one line each, no secrets).
3. **Context injection** — Confirm `agent.skip_context_files` / `HERMES_SKIP_CONTEXT_FILES` are **not** disabling governance injection (state how you know: config or operator assurance). List which of these files you **read from disk this session** with tools: `HERMES_HOME/.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `HERMES_HOME/SOUL.md` (or `WORKSPACE/SOUL.md` if linked). For **each file you read**, give the **exact first line** or **first markdown heading** as it appears on disk (verbatim, ≤200 chars) — this proves you read the live files, not memory.
4. **This session’s policy reads** — Bullet list: `path` → one-line purpose of what you used from it.
5. **System prompt / pack** — You cannot dump the full system prompt. Instead: state that Hermes assembled the usual governance + workspace pack for this profile, and name **three section titles or topic phrases** you are following from the activation handoff or loaded workspace rules (not generic AI boilerplate).

---END VERIFICATION---

# Phased activation — Sessions 1–20 with cumulative cover + carry-in + handoff
# =============================================================================
# One **new** Hermes / CLI / messaging chat per numbered session. Paste **one** block per session
# (cover + carry-in + task + handoff rules + verification suffix from the top of this file).
#
# **Order (cost controls early):** Sessions **1–2** are token governance **policy** and **Hermes
# runtime YAML** so caps, tier placeholders, and delegation limits apply before the heavy
# **Session 3** runtime-activation audit (`deployment-handoff.md`).
#
# **Prerequisite for Session 1:** Active Hermes profile; `HERMES_HOME/.hermes.md` exists with
# `POLICY_ROOT` and workspace paths; `WORKSPACE/operations/` exists (run
# `scripts/core/materialize_policies_into_hermes_home.sh` for this profile if the tree is empty).
#
# **Hermes internals (reimplementation):** `POLICY_ROOT/core/hermes-model-delegation-and-tier-runtime.md`.
#
# Agent: **Handoff** (A + B, numbered) before **Verification** is mandatory each session.
#
# **Legacy templates removed:** `runtime_activation_session1_operator_message.txt` and
# `runtime_activation_session3_operator_message.txt` — use **Session 3 COMPACT** (below) if
# context is tight, else the full **Session 3** block. Session 1 is only a deprecation pointer
# in those old files; phased order is already defined above.

# ── Session 1 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 1---

**Cover — phased activation (fresh context)**  
We are starting a **20-step procedural activation** of the agentic-company policy pack. **This session = Session 1.** Load **token / model / tool / channel governance policy** (read-only summary of binding rules) so Session 2 can implement registries and `hermes_token_governance.runtime.yaml` with the right tier story. This chat has **no prior turn history** — use **disk** (`POLICY_ROOT`, `WORKSPACE/`) and the operator carry-in only.

**Prerequisite:** Profile + `.hermes.md` + materialized `WORKSPACE/operations/` (see file header above). If those are missing, state that in **Handoff A** and stop after documenting gaps — do not invent paths.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from materialization / bootstrap notes — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/governance/standards/token-model-tool-and-channel-governance-policy.md` via tools; summarize binding rules for this deployment.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 2)** — Numbered list of **executable** tasks for the **next** activation chat: implement token governance per `POLICY_ROOT/core/governance/role-prompts/implement-token-model-and-tool-and-channel-governance-prompt.md`; install **`WORKSPACE/operations/hermes_token_governance.runtime.yaml`** from `workspace/memory/runtime/tasks/templates/script-templates/hermes_token_governance.runtime.example.yaml` in the repo checkout; set `enabled: true`. Cite exact paths under `WORKSPACE/` / `WORKSPACE/operations/`.

**Verification** — Then add **"Verification"** with: (1) absolute `HERMES_HOME` + profile name; (2) read `.hermes.md` and print policy root + workspace paths (one line each); (3) confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; (4) bullet list this session’s policy paths + one-line purpose; (5) three topic phrases from the handoff/workspace rules you are following (no full system prompt dump).

---END SESSION 1---

# ── Session 2 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 2---

**Cover — phased activation (fresh context)**  
**Session 1 is done** (token governance policy summary). This chat has **no prior turns** — use disk, registers, and **Operator carry-in**.

**This session = Session 2.** It **implements** Session 1’s policy: registries/templates under `WORKSPACE/` and `WORKSPACE/operations/` only (per implement prompt). You also **wire Hermes** to enforce caps: copy `workspace/memory/runtime/tasks/templates/script-templates/hermes_token_governance.runtime.example.yaml` to **`WORKSPACE/operations/hermes_token_governance.runtime.yaml`**, set `enabled: true`, and tune `default_model`, `blocked_model_substrings`, `max_agent_turns`, `delegation_max_iterations`, `tier_models`, and (optionally) `consultant_routing` / dynamic tier keys. From this file onward, every `AIAgent` start loads that YAML and applies it (unless `HERMES_TOKEN_GOVERNANCE_DISABLE=1`).


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 1 closing handoff — or write “None.”)**  

(paste here)

**Session task**

1. Read `POLICY_ROOT/core/governance/role-prompts/implement-token-model-and-tool-and-channel-governance-prompt.md` via tools; implement required registries/templates under `WORKSPACE/` and `WORKSPACE/operations/` only.  
2. **Runtime enforcement (required):** Install **`WORKSPACE/operations/hermes_token_governance.runtime.yaml`** from `workspace/memory/runtime/tasks/templates/script-templates/hermes_token_governance.runtime.example.yaml` in this repo checkout (adjust `default_model` / blocklist / caps / `tier_models` to match your deployment and **Session 1** summary). Set `enabled: true`. Confirm the file exists on disk at the path Hermes resolves under **`HERMES_HOME`** (profile-aware). After this, Hermes applies these caps on every agent run until disabled — this is how token governance stops being “paper only.”  
3. Optionally restart gateway / CLI session so the next message uses the new caps; note any operator env overrides (`HERMES_GOVERNANCE_ALLOW_PREMIUM`, `HERMES_TOKEN_GOVERNANCE_DISABLE`) in **Handoff A** if used.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 3)** — Numbered **executable** tasks; planned focus: **full runtime activation** — read `POLICY_ROOT/core/deployment-handoff.md` from "## Runtime Activation Prompt" through Required output; preflight and security classification per that section (directors/project leads only if the checklist requires in this phase).

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump). **Also:** state the absolute path to `hermes_token_governance.runtime.yaml` and confirm `enabled:` / key caps (`max_agent_turns`, `default_model` or tier resolution) match what you deployed.

---END SESSION 2---

# ── Session 3 COMPACT (optional — use if full Session 3 block is too large for context) ──
# Paste **one** of the blocks below instead of the full Session 3 section. Still append the
# global **---BEGIN VERIFICATION---** suffix from the top of this file. Do **not** paste all of
# `deployment-handoff.md` into chat.
---BEGIN SESSION 3 COMPACT---

**Cover — phased activation (fresh context)**  
**This session = Session 3** (runtime activation only, after Sessions 1–2). No prior turns — use disk + **Operator carry-in**.

**Session task (MINIMAL)**

Session 3 only (runtime activation). Read policy-root `core/deployment-handoff.md` from disk; run "## Runtime Activation Prompt" verification and preflight; output that section's **Required output** bullets. Paths: `HERMES_HOME/.hermes.md`.

Before **Verification**: output a top-level **Handoff** for the operator to paste into Session 4 — **A. Outstanding tasks** (numbered; `1. None.` if none) and **B. Next session (Session 4)** (numbered executable steps: `POLICY_ROOT/core/governance/artifacts-and-archival-memory.md`, `POLICY_ROOT/core/agentic-company-deployment-pack.md`; confirm `WORKSPACE/operations/`). Prose-only lists are incomplete; skipping **Handoff** is incomplete.

Then output **Verification** (same five parts as Session 1 in this file, abbreviated if needed).

---END SESSION 3 COMPACT---

---BEGIN SESSION 3 COMPACT STANDARD---

**Cover — phased activation (fresh context)**  
**This session = Session 3** (runtime activation only). No prior turns — use disk + **Operator carry-in**.

**Session task (STANDARD)**

You are the Chief Orchestrator for Session 3 (runtime activation only).

1. Open `HERMES_HOME/.hermes.md` and note `POLICY_ROOT` and workspace paths (do not paste them back at length).
2. Using file tools, read `POLICY_ROOT/core/deployment-handoff.md` starting at "## Runtime Activation Prompt" — in sections if the file is large. Do not ask the operator to paste this document.
3. Follow "Hermes runtime discipline" and the activation checklist through verification, preflight, and security classification for this session only. Do not stand up directors or project leads unless the checklist explicitly requires naming them.
4. End with the "Required output" bullet list from that section and one line: next session = Session 4 per the "Session-by-session prompt order" table in the same file.
5. **Handoff (required)** — Before **Verification**, output a section titled **Handoff** with **A. Outstanding tasks** and **B. Next session (Session 4)** as bold subsection titles on their own lines, each followed by a numbered list (not one paragraph). **A** covers blockers, deferrals, and human steps (`1. None.` if nothing open). **B** lists numbered executable tasks for the next chat (`POLICY_ROOT/core/governance/artifacts-and-archival-memory.md`, `POLICY_ROOT/core/agentic-company-deployment-pack.md`; confirm `WORKSPACE/operations/`). The operator copies **Handoff** into Session 4 carry-in; skipping **Handoff** or omitting numbered A/B is incomplete Session 3.
6. **Verification** — Absolute `HERMES_HOME` and profile name; read `.hermes.md` and print policy and workspace absolute paths (one line each); confirm `skip_context_files` is not disabling injection; list context files read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with verbatim first line or first heading from disk for each; bullet list this session's policy paths read with one-line purpose each; name three topic phrases from handoff/workspace rules you are following (do not dump the full system prompt).

---END SESSION 3 COMPACT STANDARD---

# ── Session 3 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 3---

**Cover — phased activation (fresh context)**  
**Sessions 1–2 complete:** token governance artifacts on disk **and** Hermes runtime enforcement via `workspace/operations/hermes_token_governance.runtime.yaml` when Session 2 succeeded. This chat is **empty of prior turns** — continuity from **files, registers**, and **Operator carry-in**.

**This session = Session 3.** It runs the **full runtime activation** checklist in `POLICY_ROOT/core/deployment-handoff.md` ("## Runtime Activation Prompt"): verification, preflight, security classification, Required output. **Cost caps from Session 2 should already apply** to this run. Do **not** redo Sessions 1–2 unless you find token governance files missing or invalid — fix in **Handoff A** instead. **Do not** paste all of `deployment-handoff.md` into chat — read it from disk with tools.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 2 closing handoff — or write “None.”)**  

(paste here)

**Session task**

1. Open `HERMES_HOME/.hermes.md` and note `POLICY_ROOT` and workspace paths (do not paste them back at length).  
2. Using file tools, read `POLICY_ROOT/core/deployment-handoff.md` starting at **"## Runtime Activation Prompt"** — in sections if large. Do not ask the operator to paste this document.  
3. Follow **Hermes runtime discipline** and that checklist through verification, preflight, and security classification for this session. Do not stand up directors or project leads yet unless the checklist explicitly requires naming them in this phase.  
4. End with the **Required output** bullet list from that section and **one line**: next session = Session 4 per the "Session-by-session prompt order" table in the same file.  
5. Read `POLICY_ROOT/core/hermes-model-delegation-and-tier-runtime.md` if you need a concise map of Hermes tier/delegation internals (optional, tools only).

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 4)** — Numbered list of **executable** tasks for the **next** activation chat: concrete `POLICY_ROOT/...` paths — `core/governance/artifacts-and-archival-memory.md`, `core/agentic-company-deployment-pack.md`; confirm `WORKSPACE/operations/` layout. Planned focus: **artifacts, archival memory, deployment pack constitution**.

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 3---

# ── Session 4 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 4---

**Cover — phased activation (fresh context)**  
**Sessions 1–3 complete:** token governance enforced, runtime activation checklist executed. This chat has **no prior turn history** — treat **disk** as source of truth: `WORKSPACE/operations/` registers, Session 3 outputs, and `POLICY_ROOT` policies.

**This session = Session 4.** It **builds on** Session 3 by locking in **artifacts, archival memory, and the deployment pack constitution** so later sessions (security, chief, org roles) have stable file/register contracts.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 3 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/governance/artifacts-and-archival-memory.md` and `POLICY_ROOT/core/agentic-company-deployment-pack.md` via tools; confirm `workspace/operations/` and archival layout; patch registers only if missing.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 5)** — Numbered list of **executable** tasks for the **next** activation chat: concrete `POLICY_ROOT/...` and `WORKSPACE/...` paths, files to read or patch, register updates. The next agent has **no** thread history. Planned focus: **security prompts + Chief Security Governor / audit posture** (`POLICY_ROOT/core/security-prompts.md`).

**Verification** — Then add **"Verification"** with: (1) absolute `HERMES_HOME` + profile name; (2) read `.hermes.md` and print policy root + workspace paths (one line each); (3) confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; (4) bullet list this session’s policy paths + one-line purpose; (5) three topic phrases from the handoff/workspace rules you are following (no full system prompt dump).

---END SESSION 4---

# ── Session 5 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 5---

**Cover — phased activation (fresh context)**  
**Sessions 1–4 are done:** token governance + runtime activation + artifacts/archival + canonical deployment pack alignment. This chat is **empty of prior turns** — load continuity from **files and registers** and the **operator carry-in** below.

**This session = Session 5.** It **builds on** 1–4 by activating the **security layer** (Chief Security Governor posture, audit framing) using `security-prompts.md`, so org and role sessions later inherit a classified security baseline.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 4 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/security-prompts.md` in chunks; run Chief Security Governor + audit posture for this runtime; summarize findings.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 6)** — Numbered **executable** tasks for the next chat (paths, register filenames, commands). Planned focus: **chief orchestrator directive** (`POLICY_ROOT/core/chief-orchestrator-directive.md`).

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 5---

# ── Session 6 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 6---

**Cover — phased activation (fresh context)**  
**Sessions 1–5 complete:** through security prompts/audit posture. **No chat history here** — use registers, `WORKSPACE/`, policy on disk, and carry-in.

**This session = Session 6.** It **builds on** prior work by fixing **orchestrator doctrine** (chief directive): org stance, which roles to register next, and how later “lean org” sessions will align.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 5 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/chief-orchestrator-directive.md` via tools; state org stance, initial roles to register, next step.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 7)** — Numbered **executable** tasks; planned focus: **minimal default deployment order** and reconciling `ORG_REGISTRY` / `ORG_CHART` with **Session 6** — `POLICY_ROOT/core/governance/role-prompts/minimal-default-deployment-order.md`. Assume **`WORKSPACE/operations/hermes_token_governance.runtime.yaml`** is present and `enabled: true` unless **Handoff A** says otherwise.

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 6---

# ── Session 7 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 7---

**Cover — phased activation (fresh context)**  
**Sessions 1–6 complete:** token governance (policy + Hermes runtime YAML), **Session 3** runtime activation, artifacts/pack, **security**, and **chief directive**. Fresh chat.

**This session = Session 7.** It **builds on** Session 6 by reconciling **minimal default deployment order** with orchestrator stance and **ORG_REGISTRY / ORG_CHART**.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 6 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/governance/role-prompts/minimal-default-deployment-order.md` via tools; align `ORG_REGISTRY` / `ORG_CHART` with Session 6.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 8)** — Numbered **executable** tasks; planned focus: **org-mapper / HR** standard + controller (`POLICY_ROOT/core/governance/standards/org-mapper-hr-policy.md` + role prompt).

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 7---

# ── Session 8 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 8---

**Cover — phased activation (fresh context)**  
**Sessions 1–7 complete:** org chart alignment started. No history — files and carry-in.

**This session = Session 8.** It **builds on** org registers by applying **org-mapper / HR** standard + controller to operations files.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 7 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/governance/standards/org-mapper-hr-policy.md` then `POLICY_ROOT/core/governance/role-prompts/org-mapper-hr-controller.md` via tools; apply to registers.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 9)** — Numbered **executable** tasks; planned focus: **agent lifecycle / org hygiene** (standard + controller prompts).

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 8---

# ── Session 9 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 9---

**Cover — phased activation (fresh context)**  
**Sessions 1–8 complete:** HR/mapper pass applied. Fresh context.

**This session = Session 9.** It **builds on** org structure by enforcing **agent lifecycle and org hygiene** (standards + controller).


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 8 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/governance/standards/agent-lifecycle-org-hygiene-policy.md` then `POLICY_ROOT/core/governance/role-prompts/agent-lifecycle-org-hygiene-controller.md` via tools; apply.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 10)** — Numbered **executable** tasks; planned focus: **task state and evidence** (standard + enforcer).

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 9---

# ── Session 10 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 10---

**Cover — phased activation (fresh context)**  
**Sessions 1–9 complete:** lifecycle hygiene. Empty thread.

**This session = Session 10.** It **builds on** governance by applying **task state and evidence** rules (standard + enforcer role prompt).


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 9 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/governance/standards/task-state-and-evidence-policy.md` then `POLICY_ROOT/core/governance/role-prompts/task-state-evidence-enforcer.md` via tools; apply.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 11)** — Numbered **executable** tasks; planned focus: **board of directors review** (standard + role prompt).

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 10---

# ── Session 11 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 11---

**Cover — phased activation (fresh context)**  
**Sessions 1–10 complete:** task/evidence layer. Fresh chat.

**This session = Session 11.** It **builds on** prior registers by adding **board review** policy + role workflow.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 10 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/governance/standards/board-of-directors-review-policy.md` then `POLICY_ROOT/core/governance/role-prompts/board-of-directors-review.md` via tools; apply.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 12)** — Numbered **executable** tasks; planned focus: **channel architecture** (standard + planner).

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 11---

# ── Session 12 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 12---

**Cover — phased activation (fresh context)**  
**Sessions 1–11 complete:** board review. No prior turns.

**This session = Session 12.** It **builds on** org + comms governance by planning **channel architecture** (standard + planner prompt).


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 11 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/governance/standards/channel-architecture-policy.md` then `POLICY_ROOT/core/governance/role-prompts/future-channel-architecture-planner.md` via tools; apply.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 13)** — Numbered **executable** tasks; planned focus: **client deployment** intake (standard + template).

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 12---

# ── Session 13 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 13---

**Cover — phased activation (fresh context)**  
**Sessions 1–12 complete:** channel architecture. Fresh context.

**This session = Session 13.** It **builds on** the stack by applying **client deployment** intake (standard + template).


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 12 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/governance/standards/client-deployment-policy.md` then `POLICY_ROOT/core/governance/role-prompts/client-intake-deployment-template.md` via tools; apply.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 14)** — Numbered **executable** tasks; planned focus: **agentic-company-template** and optional **markdown playbooks**.

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 13---

# ── Session 14 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 14---

**Cover — phased activation (fresh context)**  
**Sessions 1–13 complete:** client deployment patterns. Empty thread.

**This session = Session 14.** It **builds on** the company template layer: **agentic-company-template** and optional **markdown playbooks** under workspace rules.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 13 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/governance/standards/agentic-company-template.md` via tools; if playbooks needed, read `POLICY_ROOT/core/governance/role-prompts/markdown-playbook-generator.md` and generate under workspace rules.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 15)** — Numbered **executable** tasks; planned focus: **functional director** (per function; repeat Session 15 per director if needed).

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 14---

# ── Session 15 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 15---

**Cover — phased activation (fresh context)**  
**Sessions 1–14 complete:** template/playbook layer. Fresh chat. **Repeat this session once per director function** you are standing up (new chat each time if multiple functions).

**This session = Session 15.** It **builds on** ORG_CHART / registers by instantiating a **functional director** (substitute the function name in the template).


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 14 or prior Session 15 closing handoff — or write “None.”)**  

(paste here)

**Session task**

(Per director function) Read `POLICY_ROOT/core/governance/standards/functional-director-policy-template.md` then `POLICY_ROOT/core/governance/role-prompts/functional-director-template.md`; substitute function name; update registers.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session** — Numbered list. If more directors remain: frame **B** as **another Session 15** and name the function + concrete template paths. If directors are done: use **B. Next session (Session 16)** with **executable** project-lead tasks (name the project slug and template paths).

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 15---

# ── Session 16 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 16---

**Cover — phased activation (fresh context)**  
**Sessions 1–15 complete** (or 15 repeated per director). No history. **Repeat per real project** — new chat per project if several.

**This session = Session 16.** It **builds on** org structure by defining a **project lead** for a concrete project slug.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 15 or prior Session 16 closing handoff — or write “None.”)**  

(paste here)

**Session task**

(Per real project) Read `POLICY_ROOT/core/governance/standards/project-lead-policy-template.md` then `POLICY_ROOT/core/governance/role-prompts/project-lead-template.md`; apply for project slug.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session** — Numbered list. If more projects: **another Session 16** with slug + paths. Else if supervisor needed: **Session 17** steps; else skip to **Session 18** or **19** with explicit numbered routing and paths.

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 16---

# ── Session 17 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 17---

**Cover — phased activation (fresh context)**  
**Sessions 1–16 complete** (or 16 per project). Fresh context. **Only if complexity warrants a supervisor.**

**This session = Session 17.** It **builds on** project structure by adding a **supervisor** layer where justified.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 16 closing handoff — or write “None.”)**  

(paste here)

**Session task**

(If needed) Read `POLICY_ROOT/core/governance/standards/supervisor-policy-template.md` then `POLICY_ROOT/core/governance/role-prompts/supervisor-template.md`; apply.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 18)** — Numbered **executable** tasks if worker/specialist roles are needed; otherwise numbered steps jumping to **Session 19** (runtime pack / `WORKSPACE/BOOTSTRAP.md`) and say why 18 is skipped.

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 17---

# ── Session 18 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 18---

**Cover — phased activation (fresh context)**  
**Sessions 1–17 complete** (or skipped 17). Empty thread. **Only when delegating narrow worker/specialist roles.**

**This session = Session 18.** It **builds on** project/supervisor structure by applying **worker/specialist** templates.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 17 closing handoff — or write “None.”)**  

(paste here)

**Session task**

(If delegating) Read `POLICY_ROOT/core/governance/standards/worker-specialist-policy-template.md` then `POLICY_ROOT/core/governance/role-prompts/worker-specialist-template.md`; apply.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 19)** — Numbered **executable** tasks; planned focus: **`WORKSPACE/BOOTSTRAP.md`** plus `USER.md` / `IDENTITY.md` / `TOOLS.md` / `MEMORY.md`.

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 18---

# ── Session 19 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 19---

**Cover — phased activation (fresh context)**  
**Sessions 1–18 complete** (adjust for skipped optional sessions). Fresh chat.

**This session = Session 19.** It **builds on** everything prior by **customizing the runtime agent markdown pack** at `WORKSPACE/` (identity, tools, memory, user-facing files) per `BOOTSTRAP.md`.


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 18 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Follow `WORKSPACE/BOOTSTRAP.md` checklist; edit `WORKSPACE/USER.md`, `IDENTITY.md`, `TOOLS.md`, `MEMORY.md` as needed.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Next session (Session 20)** — Numbered **executable** tasks; planned focus: **gateway watchdog** + production messaging alignment — `POLICY_ROOT/core/gateway-watchdog.md`.

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 19---

# ── Session 20 ────────────────────────────────────────────────────────────────
---BEGIN SESSION 20---

**Cover — phased activation (fresh context)**  
**Sessions 1–19 complete:** runtime pack tuned. This is the **final numbered activation chat** for production messaging hygiene.

**This session = Session 20.** It **builds on** the full deployment by aligning **gateway watchdog** practice with what the operator runs on the VPS (`watchdog-check`, systemd, scripts).


**Activation protocol (non-optional):** After your main work, you **must** output **Handoff** *before* **Verification**. Inside **Handoff**, use **two** bold subsection titles on their own lines — **A. Outstanding tasks** and **B.** as specified below — each immediately followed by a **numbered** list (`1.`, `2.`, …). Do **not** fold A/B into a narrative summary or into **Verification**. The operator copies **Handoff** into the next chat’s **Operator carry-in**; write A and B so a **fresh-context** agent can execute B without reading this thread.

**Operator carry-in (paste from Session 19 closing handoff — or write “None.”)**  

(paste here)

**Session task**

Read `POLICY_ROOT/core/gateway-watchdog.md`; confirm `hermes gateway watchdog-check` and external watchdog/systemd scripts match production messaging.

**Handoff + Verification (mandatory order)**  
**Incomplete session** if you skip a top-level **Handoff** heading, omit **A** or **B**, or replace numbered lists with a vague summary. Reply order: substantive work → **Handoff** (below) → **Verification**.

**Handoff**

**A. Outstanding tasks** — Numbered list (`1.`, `2.`, …): blockers, deferred work, human-only steps, risks, open register/policy gaps. If nothing remains: `1. None.`

**B. Post-activation (no Session 21)** — Numbered **ongoing** operational tasks (monitoring, token rotation, gateway upgrades, etc.). State clearly: **numbered activation sequence complete.**

**Verification** — Same five parts as Session 1: absolute `HERMES_HOME` + profile name; read `.hermes.md` and print policy root + workspace paths (one line each); confirm `skip_context_files` is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each read; bullet this session’s policy paths + one-line purpose; three topic phrases from handoff/workspace rules (no full system prompt dump).

---END SESSION 20---
