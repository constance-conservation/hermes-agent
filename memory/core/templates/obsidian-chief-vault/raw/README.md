# Raw layer

`workspace/` here is a **mirror** of `${HERMES_HOME}/workspace/` produced by `sync_chief_workspace_to_obsidian_vault.sh`.

Treat as **source-of-truth read-only** for wiki operations. Edits in the live workspace should be made under `HERMES_HOME/workspace/`; then re-run the sync.
