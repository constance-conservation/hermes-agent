# Overview

This vault mirrors **`${HERMES_HOME}/workspace`** under `raw/workspace/` and holds a growing **wiki** layer for compounding knowledge.

- **Raw**: synced copy — do not hand-edit; refresh with `sync_chief_workspace_to_obsidian_vault.sh`.
- **Wiki**: LLM-maintained notes, links, and synthesis — safe to edit in session workflows.

See [[SCHEMA]] (vault root) and [[index]].
